import S from './style';

type FlexTableProps = {
  
};

const FlexTable = ({}: FlexTableProps) => {
    return (
        <S.Wrapper>
          
        </S.Wrapper>
    );
};

export default FlexTable;