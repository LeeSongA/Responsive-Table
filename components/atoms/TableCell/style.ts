import styled from 'styled-components';

const S = {
  Wrapper: styled.span`
    height: 40px;
    line-height: 40px;
    border-top: 1px solid #000000;
    border-left: 1px solid #000000;
    align-self: stretch;
    padding-left: 12px;
  `
} 

export default S;
